import React from 'react'
import ReactDOM from 'react-dom/client'
import { HistoryPickerApp } from './HistoryPickerApp'
import '../src/index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <HistoryPickerApp />
  </React.StrictMode>
)
